-- Add admin user (replace with actual user ID after first signup)
-- This script should be run after creating an admin user account
-- INSERT INTO public.admin_access (user_id, is_admin)
-- VALUES ('USER_ID_HERE', true);

-- Example: To find your user ID, run this query in the Supabase dashboard:
-- SELECT id, email FROM auth.users WHERE email = 'admin@example.com';
-- Then replace USER_ID_HERE with the actual UUID
